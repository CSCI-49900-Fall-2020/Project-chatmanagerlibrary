/*
    This class holds all the commands for Teams
*/ 
class TeamsCommands {
    /*
        Constructor 
    */
    constructor(bot) {             
       
    }

    /*
        This function sends direct message to another user
        @params: string(message content)
        returns success or error
    */
    function sendDirectMessage(message){

    }
    /*
        This function sends file to another user
        @params: string(message content)
        returns success or error
    */

    function sendFile(file){
        
    }
    
  }

  module.exports = TeamsCommands;